function Request(props) {
    
    return (
        <div>

            <button className= "buttontype2" onClick={props.movies}>Show movies (please note that it may take a while for the movies to show)</button>

        </div>
    
    )
}

export default Request;